// 7. Hacer una funcion que reciba un nombre, si existe nombre saludar por nombre de otra manera saludar 'Hola, desconocido'

/**let nameInput = document.getElementById('nombre');
console.log(nameInput);

let saludoP = document.querySelector('mostrarSaludo');
console.log(saludoP);

//Acceder al contenido de nuestras variables
let name = nameInput.value;

function saludarUsuario() {
    //acceder al contenido de nuestras variables:
    let name =nameInput.value;

    console.log(name);
    let mensaje = '';
    if(name){
        mensaje = 'Hola' + '' + name;
        // Text content me permite cambiar el texto dentro de etiquetas
        saludoP.textContent = mensaje;
    }else {
        mensaje = 'Hola desconocido'
        saludoP.textContent = mensaje;
    }
} */
